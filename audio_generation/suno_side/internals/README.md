Suno Bark TTS internals

Setup (CPU):
- Ensure system ffmpeg and libsndfile are installed.
- Create environment with uv or venv, then install dependencies from pyproject.

Run test:
- From this directory, run via project driver: `python ../driver.py`


